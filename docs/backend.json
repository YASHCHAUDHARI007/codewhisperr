{
  "entities": {
    "Project": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Project",
      "type": "object",
      "description": "Represents a codebase uploaded by a user for AI analysis, either via a ZIP file or a GitHub repository URL.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Project entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this project. This ID comes from an external authentication system. (Relationship: User 1:N Project)"
        },
        "name": {
          "type": "string",
          "description": "The name of the project, typically derived from the GitHub repository name or the uploaded ZIP file name."
        },
        "sourceUrl": {
          "type": "string",
          "description": "The URL of the GitHub repository if the project was ingested from a URL.",
          "format": "uri"
        },
        "uploadFileName": {
          "type": "string",
          "description": "The original file name if the project was ingested via a ZIP file upload."
        },
        "status": {
          "type": "string",
          "description": "The current processing status of the project (e.g., 'pending', 'processing', 'analyzed', 'failed')."
        },
        "uploadDate": {
          "type": "string",
          "description": "The timestamp when the project was initially uploaded or linked.",
          "format": "date-time"
        },
        "lastAnalysisDate": {
          "type": "string",
          "description": "The timestamp of the last successful AI analysis of the project.",
          "format": "date-time"
        },
        "summary": {
          "type": "string",
          "description": "AI-generated high-level summary of what the project does."
        },
        "techStack": {
          "type": "array",
          "description": "AI-identified technologies and frameworks used in the project.",
          "items": {
            "type": "string"
          }
        },
        "architecture": {
          "type": "string",
          "description": "AI-described overview of the project's architectural patterns and components."
        },
        "codeQualityScore": {
          "type": "number",
          "description": "An optional numerical score representing the overall code quality of the project, if generated by AI."
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "status",
        "uploadDate"
      ]
    },
    "CodeFile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CodeFile",
      "type": "object",
      "description": "Represents an individual code file or module extracted from an analyzed project, along with its AI-generated insights.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CodeFile entity."
        },
        "projectId": {
          "type": "string",
          "description": "Reference to the Project this file belongs to. (Relationship: Project 1:N CodeFile)"
        },
        "filePath": {
          "type": "string",
          "description": "The full path of the file relative to the project's root directory."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the file (e.g., 'index.js')."
        },
        "fileExtension": {
          "type": "string",
          "description": "The file extension (e.g., 'js', 'py', 'java')."
        },
        "fileContent": {
          "type": "string",
          "description": "The raw content of the code file. This can be large and may require careful storage/retrieval strategies."
        },
        "explanation": {
          "type": "string",
          "description": "AI-generated explanation or summary of the file's purpose and functionality."
        },
        "detectedIssues": {
          "type": "array",
          "description": "An array of AI-detected potential bugs, bad practices, or inefficiencies within this file.",
          "items": {
            "type": "string"
          }
        },
        "suggestedImprovements": {
          "type": "array",
          "description": "An array of AI-suggested improvements or fixes for issues in this file.",
          "items": {
            "type": "string"
          }
        },
        "lastAnalyzedDate": {
          "type": "string",
          "description": "The timestamp of the last AI analysis for this specific file.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "projectId",
        "filePath",
        "fileName",
        "fileContent"
      ]
    },
    "ChatSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatSession",
      "type": "object",
      "description": "Represents an interactive 'Ask AI' chat conversation specific to a particular project.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatSession entity."
        },
        "projectId": {
          "type": "string",
          "description": "Reference to the Project this chat session is about. (Relationship: Project 1:N ChatSession)"
        },
        "title": {
          "type": "string",
          "description": "A descriptive title for the chat session, possibly auto-generated or user-provided."
        },
        "startDate": {
          "type": "string",
          "description": "The timestamp when the chat session was initiated.",
          "format": "date-time"
        },
        "lastActivityDate": {
          "type": "string",
          "description": "The timestamp of the last message sent or received in this chat session.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "projectId",
        "startDate"
      ]
    },
    "ChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents an individual message exchanged within an AI chat session.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatMessage entity."
        },
        "chatSessionId": {
          "type": "string",
          "description": "Reference to the ChatSession this message belongs to. (Relationship: ChatSession 1:N ChatMessage)"
        },
        "senderType": {
          "type": "string",
          "description": "Indicates who sent the message: 'user' or 'ai'."
        },
        "content": {
          "type": "string",
          "description": "The text content of the message."
        },
        "timestamp": {
          "type": "string",
          "description": "The timestamp when the message was sent.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "chatSessionId",
        "senderType",
        "content",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/projects/{projectId}",
        "definition": {
          "entityName": "Project",
          "schema": {
            "$ref": "#/backend/entities/Project"
          },
          "description": "Stores individual projects uploaded or linked by a user. Includes denormalized 'userId' for explicit ownership and authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the project, derived from Firebase Authentication."
            },
            {
              "name": "projectId",
              "description": "The unique identifier for a specific project."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/projects/{projectId}/codeFiles/{codeFileId}",
        "definition": {
          "entityName": "CodeFile",
          "schema": {
            "$ref": "#/backend/entities/CodeFile"
          },
          "description": "Stores individual code files extracted from a project, along with their AI analysis. Includes denormalized 'userId' and 'projectId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent project."
            },
            {
              "name": "projectId",
              "description": "The unique identifier of the parent project this file belongs to."
            },
            {
              "name": "codeFileId",
              "description": "The unique identifier for a specific code file within a project."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/projects/{projectId}/chatSessions/{chatSessionId}",
        "definition": {
          "entityName": "ChatSession",
          "schema": {
            "$ref": "#/backend/entities/ChatSession"
          },
          "description": "Stores interactive AI chat sessions related to a specific project. Includes denormalized 'userId' and 'projectId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent project."
            },
            {
              "name": "projectId",
              "description": "The unique identifier of the parent project this chat session is about."
            },
            {
              "name": "chatSessionId",
              "description": "The unique identifier for a specific AI chat session."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/projects/{projectId}/chatSessions/{chatSessionId}/messages/{messageId}",
        "definition": {
          "entityName": "ChatMessage",
          "schema": {
            "$ref": "#/backend/entities/ChatMessage"
          },
          "description": "Stores individual messages within an AI chat session. Includes denormalized 'userId', 'projectId', and 'chatSessionId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent project and chat session."
            },
            {
              "name": "projectId",
              "description": "The unique identifier of the parent project the chat session belongs to."
            },
            {
              "name": "chatSessionId",
              "description": "The unique identifier of the parent chat session this message belongs to."
            },
            {
              "name": "messageId",
              "description": "The unique identifier for a specific message within a chat session."
            }
          ]
        }
      }
    ],
    "reasoning": "The recommended Firestore structure is designed following the core principles of Authorization Independence, Clarity of Intent, DBAC, QAPs, and Invariants, specifically tailored for the 'AI Codebase Explainer & Debugger' application. It prioritizes a path-based ownership model, where all user-specific data is nested under a `/users/{userId}` root. This design ensures robust security rules that are simple to write and debug, and highly performant for common access patterns.\n\n**Authorization Independence via Denormalization:**\nThis is a critical aspect of the design. For `Project` documents, ownership is inherently established by the `userId` field within the document and by its placement under `/users/{userId}/projects/{projectId}`. For subcollections like `CodeFile`, `ChatSession`, and `ChatMessage`, authorization independence is achieved by denormalizing the `userId` (and `projectId` for `ChatSession` and `ChatMessage`, and `chatSessionId` for `ChatMessage`) into each document. For example, a `CodeFile` document at `/users/{userId}/projects/{projectId}/codeFiles/{codeFileId}` will explicitly contain `userId` and `projectId` fields. This allows Firestore Security Rules to authorize access to any `CodeFile` document by simply checking `request.auth.uid == resource.data.userId` and validating the path segments (e.g., `userId == request.auth.uid && projectId == resource.data.projectId`). This eliminates the need for expensive and potentially insecure `get()` calls to parent documents in rules, making operations atomic and easier to reason about. It also ensures that a document's authorization context is self-contained.\n\n**Support for Querying and Access Patterns (QAPs):**\n1.  **Listing Projects:** Users can easily list all their projects by querying `/users/{request.auth.uid}/projects`. The security rules will automatically restrict this query to only retrieve projects owned by the authenticated user, as mandated by the `userId` path segment matching `request.auth.uid` and the denormalized `resource.data.userId` field. This structure directly enables secure `list` operations without relying on rules as filters.\n2.  **Accessing Specific Project/Files/Chats:** To access a specific project, its files, or chat sessions, the full path `/users/{request.auth.uid}/projects/{projectId}/...` will be used. The denormalized `userId` and `projectId` (and `chatSessionId` for messages) within the documents, combined with the path segments, allow for efficient and secure point reads and writes. The rules can verify that the `uid` in the path matches `request.auth.uid` and that the `projectId` in the path matches the `projectId` in the document data, ensuring data integrity and ownership.\n\n**Structural Segregation and Predictability:**\nEach collection (projects, codeFiles, chatSessions, messages) maintains a homogeneous security posture: all documents within these collections are private to the owning user. This simplifies rule writing significantly. Descriptive wildcards like `{userId}`, `{projectId}`, `{codeFileId}`, `{chatSessionId}`, and `{messageId}` are used for clarity. The use of a `status` field for `Project` (e.g., 'pending', 'analyzed') provides explicit state modeling, enhancing debuggability and predictability of the application's logic. This consistent and hierarchical structure ensures the application can scale securely and efficiently while maintaining a clear and debuggable authorization model."
  }
}